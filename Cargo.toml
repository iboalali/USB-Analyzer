[workspace]
resolver = "2"
members = ["crates/usb-probe", "crates/usbdiag"]

[workspace.package]
version = "0.1.0"
edition = "2021"
# 1.82 for Option::is_none_or, which the model and the rule engine both use.
rust-version = "1.82"
license = "MIT"

[workspace.dependencies]
usb-probe = { path = "crates/usb-probe" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = "thin"
strip = true
